package com.wallpapercropfixer.presentation.components

// Removed — focus-point overlay is now integrated directly into DevicePreviewFrame
// so the overlay Canvas shares the exact layout bounds of the image, eliminating
// the coordinate-mapping mismatch that existed when this was a separate composable.
// See DevicePreviewFrame.kt for the unified implementation.
